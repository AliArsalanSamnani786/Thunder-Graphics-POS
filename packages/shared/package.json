{
  "name": "@thunder-pos/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "lint": "npx tsc -p tsconfig.json --noEmit",
    "typecheck": "npx tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

