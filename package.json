{
  "name": "thunder-pos",
  "version": "0.1.0",
  "private": true,
  "description": "Thunder POS enterprise multi-tenant SaaS platform.",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "postinstall": "corepack pnpm run prisma:generate && corepack pnpm --filter @thunder-pos/shared build",
    "dev": "corepack pnpm --parallel --filter @thunder-pos/web --filter @thunder-pos/api dev",
    "clean": "node scripts/clean-workspaces.mjs",
    "build": "node scripts/clean-workspaces.mjs && corepack pnpm --filter @thunder-pos/shared build && corepack pnpm --filter @thunder-pos/api build && corepack pnpm --filter @thunder-pos/web build",
    "lint": "corepack pnpm -r lint",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "corepack pnpm -r test",
    "prisma:generate": "prisma generate",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prisma": "^5.22.0"
  }
}
